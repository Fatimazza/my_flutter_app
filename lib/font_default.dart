import 'package:flutter/material.dart';

class FontDefaultPage extends StatelessWidget {
  const FontDefaultPage({super.key});
  
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
